<VirtualHost *:80>
    ServerAdmin root@localhost
    DocumentRoot /var/www/html/rl
    ServerName  ralphlauren-us.dev.sapient.com
    ErrorLog logs/ralphlauren-us.dev.sapient.com-error_log
    CustomLog logs/ralphlauren-us.dev.sapient.com-access_log common

#        RewriteEngine on
        # This checks to make sure the connection is not already HTTPS
        #RewriteRule ^/myaccount?(.*) https://%{SERVER_NAME}/$1 [R,L]

        #Disable TRACE and TRACK
#        RewriteCond %{REQUEST_METHOD} ^TRACE
#        RewriteRule .* - [F]
#
#        RewriteCond %{REQUEST_METHOD} ^TRACK
#        RewriteRule .* - [F]

#JkUnMount /ralphlauren/_ui/ loadbalancer
JkMount /* loadbalancer

</VirtualHost>
